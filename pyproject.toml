[project]
name = "multinode-training-guide"
version = "0.1.0"
description = "Repo-level Python tooling for multinode-training-guide"
requires-python = ">=3.11"
dependencies = [
    "modal",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pyright>=1.1.408",
    "ruff>=0.15.11",
]
